<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.expensetracker" >

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".My_Application"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme" >
        <activity android:name=".Column_Chart_Activity" >
        </activity>
        <activity
            android:name=".Splash_Screen_Activity"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar" >

            <!-- android:name="com.example.expensetracker.Main_Activity" -->
            <!-- android:theme="@style/Theme.AppCompat.Light.NoActionBar"> -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Login_Activity"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar" />
        <activity android:name=".User_Activity" />
        <activity android:name=".New_Pass_Activity" />
        <activity
            android:name=".Main_Activity"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar" /> <!-- <activity -->
        <!-- android:name="com.example.expensetracker.Splash_Screen_Activity" -->
        <!-- android:theme="@style/Theme.AppCompat.Light.NoActionBar" /> -->
        <!-- <activity -->
        <!-- android:name="com.example.expensetracker.Splash_Screen_Activity" -->
        <!-- android:theme="@style/Theme.AppCompat.Light.NoActionBar" /> -->
        <activity android:name=".Category_Activity" />
        <activity android:name=".Transaction_Activity" />
        <activity
            android:name=".Trans_Activity"
            android:parentActivityName=".User_Activity" />
    </application>

</manifest>